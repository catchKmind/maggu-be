spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/localmap_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

server:
  port: 8080

tourism-api:
  base-url: http://apis.data.go.kr/B551011/KorService2
  service-key: ${TOURISM_API_KEY}

jwt:
  secret: f988e2a0b7efb7f0a487ff23f729aa60d747cc0a2c89aba3569d6ec58c2f2bc49fdbef3426b0cedbdf9242f4f27728f9b7a45fb993030b512bd88ba8d1d9b34a

  access-token-validity-seconds: 3600
  refresh-token-validity-seconds: 1209600

apple:
  bundle-id: ${APPLE_BUNDLE_ID:maggu.co.kr}
  team-id: ${APPLE_TEAM_ID:}
  key-id: ${APPLE_KEY_ID:}
  key-path: ${APPLE_KEY_PATH:classpath:AppleSignInsKey.p8}

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    s3:
      bucket: maggu-bucket-418316940851-ap-northeast-2-an
    cloudfront:
      domain: ${CLOUDFRONT_DOMAIN:d26phrt3m0ikhe.cloudfront.net}
    region:
      static: ap-northeast-2
    stack:
      auto: false

upload:
  presigned-url-expiration-seconds: 300